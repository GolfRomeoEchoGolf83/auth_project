<% if @current_user %>
    <% if @current_user.role == "admin" %>
        <h1>Welcome home admin
            <%= @current_user.name %>
        </h1>
        <% else %>
            <h1>Welcome
                <%= @current_user.name %>
            </h1>
            <% else %>
                <h1>Welcome anonyme user...</h1>
                <a href="/users/login">Login</a>
                <% end %>